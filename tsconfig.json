{
    "compilerOptions": {
        "target": "ESNext" /* 최신 브라우저는 es6을 대부분 지원한다. */,
        "module": "ESNext" /* 모듈 시스템을 지정한다. */,
        "lib": [
            "ESNext"
        ] /* 타입스크립트가 어떤 버전의 JS의 빌트인 API를 사용할 건지에 대한 것을 명시해 준다. */,
        "declaration": true /* 타입스크립트가 자동으로 타입정의 (d.ts) 파일을 생성해 준다. */,
        "outDir": "dist" /* 컴파일된 결과물을 어디에 저장할지에 대한 것을 명시해 준다. */,
        "strict": true /* 타입스크립트의 엄격한 모드를 활성화한다. */,
        "moduleResolution": "Node",
        "allowJs": true /* 자바스크립트 파일을 허용한다. */,
        "skipLibCheck": true /* 라이브러리 체크를 스킵한다. */,
        "esModuleInterop": true /* es 모듈을 commonjs 모듈로 변환한다. */
    },
    "include": ["src/**/*.ts"] /* 컴파일할 대상을 명시해 준다. */
}
